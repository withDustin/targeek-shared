{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna-publish": "lerna version && lerna run publish-npm",
    "clean": "lerna clean -y && rm -f {packages/*/,}yarn.lock",
    "reinstall": "yarn clean && yarn install"
  },
  "devDependencies": {
    "lerna": "3.15.0",
    "prettier": "1.18.2",
    "tslint": "5.18.0",
    "typescript": "3.5.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
